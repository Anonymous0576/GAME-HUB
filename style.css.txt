```css
:root {
    --bg: #070b14;
    --surface: #101725;
    --surface2: #151e2f;
    --border: #263248;
    --text: #f8fafc;
    --muted: #8d99ad;
    --accent: #7c5cff;
    --accent2: #a78bfa;
    --danger: #ff4d6d;
}

* {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
}

html {
    scroll-behavior: smooth;
}

body {
    background: var(--bg);
    color: var(--text);
    font-family: Arial, Helvetica, sans-serif;
}

button,
input {
    font: inherit;
}

button {
    cursor: pointer;
}

a {
    color: inherit;
    text-decoration: none;
}


/* NAVBAR */

.navbar {
    height: 72px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 0 5%;
    border-bottom: 1px solid var(--border);
    background: rgba(7,11,20,.92);
    backdrop-filter: blur(15px);
    position: sticky;
    top: 0;
    z-index: 100;
}

.brand {
    font-size: 23px;
    font-weight: 800;
}

.brand-icon {
    margin-right: 7px;
}

.navbar nav {
    display: flex;
    gap: 30px;
}

.navbar nav a {
    color: var(--muted);
    font-size: 14px;
    transition: .2s;
}

.navbar nav a:hover {
    color: white;
}

.nav-right {
    display: flex;
    gap: 8px;
}

.icon-btn {
    border: 1px solid var(--border);
    background: var(--surface);
    color: white;
    border-radius: 9px;
    width: 40px;
    height: 40px;
}


/* HERO */

.hero {
    min-height: 480px;
    display: flex;
    align-items: center;
    padding: 70px 9%;
    overflow: hidden;

    background:
        radial-gradient(
            circle at 75% 50%,
            #7c5cff30,
            transparent 35%
        ),
        radial-gradient(
            circle at 15% 10%,
            #00b7ff18,
            transparent 35%
        );
}

.hero-content {
    max-width: 650px;
    z-index: 2;
}

.hero-label,
.small-title {
    color: var(--accent2);
    font-size: 12px;
    font-weight: 800;
    letter-spacing: 2px;
}

.hero h1 {
    font-size: clamp(50px, 7vw, 88px);
    line-height: .95;
    margin: 15px 0 25px;
}

.hero h1 span {
    color: var(--accent2);
}

.hero p {
    color: var(--muted);
    font-size: 18px;
    line-height: 1.6;
    max-width: 550px;
}

.hero-button {
    display: inline-block;
    margin-top: 30px;
    padding: 15px 24px;
    background: var(--accent);
    border-radius: 10px;
    font-weight: bold;
    transition: .2s;
}

.hero-button:hover {
    transform: translateY(-3px);
}

.hero-art {
    position: relative;
    width: 430px;
    height: 350px;
    margin-left: auto;
}

.hero-controller {
    width: 220px;
    height: 220px;
    border-radius: 50%;
    background: linear-gradient(
        145deg,
        #7c5cff,
        #31236f
    );

    display: flex;
    align-items: center;
    justify-content: center;

    font-size: 90px;

    position: absolute;
    left: 100px;
    top: 60px;

    box-shadow:
        0 0 100px #7c5cff55;
}

.floating-game {
    position: absolute;
    font-size: 45px;
    padding: 18px;
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 18px;
    box-shadow: 0 15px 40px #0008;
    animation: float 3s infinite ease-in-out;
}

.game-a {
    top: 10px;
    left: 30px;
}

.game-b {
    right: 20px;
    top: 60px;
    animation-delay: .4s;
}

.game-c {
    bottom: 20px;
    left: 40px;
    animation-delay: .8s;
}

.game-d {
    bottom: 40px;
    right: 30px;
    animation-delay: 1.2s;
}

@keyframes float {

    50% {
        transform: translateY(-12px);
    }

}


/* SEARCH */

.search-section {
    margin-top: -25px;
    position: relative;
    z-index: 5;
}

.search {
    max-width: 900px;
    margin: auto;
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 13px;
    display: flex;
    align-items: center;
    padding: 5px 18px;
    box-shadow: 0 15px 40px #0006;
}

.search span {
    font-size: 20px;
}

.search input {
    flex: 1;
    background: transparent;
    border: none;
    outline: none;
    color: white;
    padding: 15px;
}

.search kbd {
    color: var(--muted);
    border: 1px solid var(--border);
    padding: 4px 8px;
    border-radius: 5px;
    font-size: 11px;
}


/* CONTAINER */

.container {
    width: 88%;
    max-width: 1400px;
    margin: auto;
}


/* CATEGORIES */

.categories-section {
    padding: 65px 0 20px;
}

.categories-section h2 {
    margin-bottom: 20px;
}

.categories {
    display: flex;
    gap: 10px;
    flex-wrap: wrap;
}

.category {
    border: 1px solid var(--border);
    background: var(--surface);
    color: var(--muted);
    padding: 11px 17px;
    border-radius: 9px;
    transition: .2s;
}

.category:hover,
.category.active {
    background: var(--accent);
    color: white;
    border-color: var(--accent);
}


/* SECTIONS */

.game-section {
    padding: 45px 0;
}

.section-heading {
    display: flex;
    align-items: end;
    justify-content: space-between;
    margin-bottom: 25px;
}

.section-heading h2 {
    font-size: 30px;
    margin-top: 5px;
}

.view-all {
    background: transparent;
    border: none;
    color: var(--accent2);
}


/* FEATURED */

.featured-grid {
    display: grid;
    grid-template-columns: 2fr 1fr 1fr;
    gap: 18px;
}

.featured-card {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 14px;
    overflow: hidden;
    transition: .25s;
}

.featured-card:hover {
    transform: translateY(-5px);
    border-color: var(--accent);
}

.game-cover {
    height: 210px;
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
}

.large .game-cover {
    height: 280px;
}

.cover-icon {
    font-size: 80px;
    filter: drop-shadow(0 15px 20px #0008);
}

.space-cover {
    background:
        radial-gradient(circle, #46339b, #11172a);
}

.racing-cover {
    background:
        linear-gradient(135deg, #173b5f, #26133d);
}

.puzzle-cover {
    background:
        linear-gradient(135deg, #13504c, #181f42);
}

.badge {
    position: absolute;
    top: 14px;
    left: 14px;
    background: var(--danger);
    padding: 5px 8px;
    border-radius: 5px;
    font-size: 10px;
    font-weight: bold;
}

.game-details {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 17px;
}

.game-details h3 {
    margin-bottom: 5px;
}

.game-details p {
    color: var(--muted);
    font-size: 13px;
}

.play-button {
    border: none;
    background: var(--accent);
    color: white;
    padding: 9px 13px;
    border-radius: 7px;
    font-size: 12px;
    font-weight: bold;
}


/* GAME GRID */

.games-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 18px;
}

.game-card {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 12px;
    overflow: hidden;
    transition: .2s;
}

.game-card:hover {
    transform: translateY(-5px);
    border-color: var(--accent);
}

.card-image {
    height: 180px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 65px;
    position: relative;
}

.snake {
    background: linear-gradient(135deg,#174e45,#172735);
}

.shooter {
    background: linear-gradient(135deg,#401a55,#17223c);
}

.racing {
    background: linear-gradient(135deg,#4c1e31,#17294c);
}

.memory {
    background: linear-gradient(135deg,#15485b,#25204e);
}

.chess {
    background: linear-gradient(135deg,#45361e,#211d19);
}

.bird {
    background: linear-gradient(135deg,#155b77,#293a65);
}

.favorite {
    position: absolute;
    top: 12px;
    right: 12px;
    background: #0008;
    width: 32px;
    height: 32px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 50%;
    font-size: 17px;
}

.card-content {
    padding: 15px;
}

.card-content h3 {
    margin-bottom: 5px;
}

.card-content p {
    color: var(--muted);
    font-size: 13px;
}

.game-bottom {
    margin-top: 15px;
    display: flex;
    align-items: center;
    justify-content: space-between;
}

.game-bottom span {
    color: #fbbf24;
    font-size: 13px;
}

.game-bottom button {
    background: var(--accent);
    color: white;
    border: none;
    border-radius: 6px;
    padding: 7px 13px;
    font-weight: bold;
}


/* NEW BANNER */

.new-game-banner {
    min-height: 220px;
    border-radius: 18px;
    padding: 45px;
    display: flex;
    align-items: center;
    justify-content: space-between;

    background:
        radial-gradient(circle at 80% 50%, #7c5cff55, transparent 30%),
        linear-gradient(120deg,#151e36,#251c42);

    border: 1px solid var(--border);
}

.new-game-banner span {
    color: var(--accent2);
    font-size: 12px;
    font-weight: bold;
    letter-spacing: 2px;
}

.new-game-banner h2 {
    font-size: 35px;
    margin: 10px 0;
}

.new-game-banner p {
    color: var(--muted);
}

.banner-icon {
    font-size: 110px;
}


/* GAME MODAL */

.game-modal {
    display: none;
    position: fixed;
    inset: 0;
    z-index: 1000;
    background: #02040acc;
    backdrop-filter: blur(10px);
    align-items: center;
    justify-content: center;
    padding: 25px;
}

.game-modal.show {
    display: flex;
}

.game-window {
    width: min(1100px, 96vw);
    height: min(750px, 90vh);
    background: #090e18;
    border: 1px solid var(--border);
    border-radius: 14px;
    overflow: hidden;
    box-shadow: 0 30px 100px #000;
}

.game-topbar {
    height: 60px;
    padding: 0 18px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    background: var(--surface);
    border-bottom: 1px solid var(--border);
}

.game-topbar strong {
    display: block;
}

.game-topbar span {
    color: var(--muted);
    font-size: 11px;
}

.game-controls {
    display: flex;
    gap: 7px;
}

.game-controls button {
    width: 35px;
    height: 35px;
    border: 1px solid var(--border);
    border-radius: 7px;
    background: var(--surface2);
    color: white;
}

.game-controls .close {
    background: #ef4444;
    border-color: #ef4444;
}

#gameContainer {
    height: calc(100% - 60px);
    display: flex;
    align-items: center;
    justify-content: center;
}

.game-start-screen {
    text-align: center;
}

.big-game-icon {
    font-size: 90px;
    margin-bottom: 20px;
}

.game-start-screen p {
    color: var(--muted);
    margin: 10px 0 25px;
}

.game-start-screen button {
    background: var(--accent);
    border: none;
    color: white;
    padding: 13px 25px;
    border-radius: 8px;
    font-weight: bold;
}


/* GAME CANVAS */

#gameCanvas {
    max-width: 95%;
    max-height: 90%;
    background: #050912;
    border: 2px solid var(--accent);
}


/* FOOTER */

footer {
    margin-top: 70px;
    padding: 50px 7%;
    text-align: center;
    border-top: 1px solid var(--border);
    color: var(--muted);
}

.footer-logo {
    color: white;
    font-size: 22px;
    font-weight: bold;
    margin-bottom: 10px;
}

.footer-links {
    display: flex;
    justify-content: center;
    gap: 25px;
    margin: 25px 0;
}

.footer-links a:hover {
    color: white;
}

.copyright {
    font-size: 12px;
}


/* RESPONSIVE */

@media(max-width: 1000px) {

    .hero-art {
        display: none;
    }

    .featured-grid {
        grid-template-columns: 1fr 1fr;
    }

    .featured-card.large {
        grid-column: span 2;
    }

    .games-grid {
        grid-template-columns: repeat(3,1fr);
    }

}

@media(max-width: 700px) {

    .navbar nav {
        display: none;
    }

    .hero {
        padding: 60px 7%;
    }

    .hero h1 {
        font-size: 55px;
    }

    .featured-grid {
        grid-template-columns: 1fr;
    }

    .featured-card.large {
        grid-column: auto;
    }

    .games-grid {
        grid-template-columns: repeat(2,1fr);
    }

}

@media(max-width: 480px) {

    .games-grid {
        grid-template-columns: 1fr;
    }

    .search kbd {
        display: none;
    }

    .new-game-banner {
        padding: 30px;
    }

    .banner-icon {
        font-size: 60px;
    }

}
```
